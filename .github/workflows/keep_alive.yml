name: Keep Streamlit Alive

on:
  schedule:
    - cron: '0 */6 * * *' # 6시간마다 실행 (원하는 주기로 변경 가능)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://wattda-p9xlugxpilazcaejh4l2zw.streamlit.app/
